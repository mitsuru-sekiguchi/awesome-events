.layouts__application
  = render partial: 'shared/header'
%h1 ユーザー管理画面

%br

%ul.list-group
  - @users.each do |user|
    = link_to(user, class: "list-group-item list-group-item-action") do
      = image_tag user.image_url, width: 50, height: 50
      %h5= user.name
      - if current_user == user || current_user.admin?
        = link_to "ユーザーを削除", user, method: :delete, data: { confirm: "本当に削除して宜しいですか？" }
%br
.col-4
  = link_to "TOPへ戻る", root_path, class: "btn btn-secondary btn-lg btn-block"
